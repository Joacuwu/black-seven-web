/** Dirección pública del sitio, sin barra final (se usa en mails, sitemap y metadatos). */
export const SITE_URL = (process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000").replace(/\/+$/, "");
export const SITE_NAME = "BLACK SEVEN";
