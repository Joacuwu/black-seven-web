// Reglas de precio compartidas entre el servidor (cobro) y el navegador (resumen del checkout).
// Si el cliente cambia estas condiciones, se cambian acá y en un solo lugar.

export const FREE_SHIPPING_THRESHOLD = 50000;
export const SHIPPING_COST = 4500;
export const TRANSFER_DISCOUNT_RATE = 0.1;
export const MAX_QUANTITY_PER_ITEM = 10;
